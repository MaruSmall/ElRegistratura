@model IEnumerable<ElRegistratura.Models.Doctor>

@{
    ViewData["Title"] = "Поиск врача";
}

<h1>Поиск врача</h1>
<form asp-controller="Home" asp-action="SearchDoctor" method="get">
    <p>
        Поиск: <input type="text" name="SearchString" />
        <input type="submit" value="Поиск" />
    </p>  
</form>

@*@using (Html.BeginForm("SearchDoctor", "HomeController"))
{
    @Html.TextBox("LastName")
    <input type="submit" value="Поиск" />
}*@

@*
@using (Html.BeginForm())
{
    @Html.LabelFor(x => x.LastName)
    @Html.EditorFor(x => x.LastName)
    @Html.ValidationMessageFor(x => x.LastName)
    <button type="submit">Search</button>
}*@

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.LastName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FirstName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Patronymic)
            </th>
            @*<th>
                @Html.DisplayNameFor(model => model.Speciality.Name)
            </th>
             <th>
                @Html.DisplayNameFor(model => model.Clinic)
            </th>*@
            
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.LastName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FirstName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Patronymic)
            </td>
           @* <td>
                 @Html.DisplayFor(modelItem => item.Speciality.Name)
            </td>
             <td>
                 @Html.DisplayFor(modelItem => item.Clinic.Name)
                 @Html.DisplayFor(modelItem => item.Clinic.Street.Name)
                 @Html.DisplayFor(modelItem => item.Clinic.HouseNumb)
                 @Html.DisplayFor(modelItem => item.Clinic.Housing)

            </td>*@
            <td>
                <a asp-action="ScheduleDoctorsView" asp-route-id="@item.Id">Расписание</a> |
               <a asp-action="Details" asp-controller="Doctors" asp-route-id="@item.Id">Подробнее</a> 
            </td>
        </tr>
}
    </tbody>
</table>
